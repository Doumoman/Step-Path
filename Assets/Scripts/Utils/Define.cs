public enum UIEvent  //enumaration : 열거형
{
    Click
}
public enum Sound
{
    Bgm,
    Effect,
    LoopEffect,
    MaxCount
}

public enum PlayterLookingDirection
{
    Left,
    Right
}

public enum PlayerState
{
    Idle, // 가만히 있음
    Walk,
    Jump,
    Fall
}